file lens.tpl-db
{
    {
        LOCATION = $(LOCATION)
        LENSID   = $(LENSID)
        Z        = $(Z)
        Z_MOT    = $(Z_MOT)
        NLENS    = $(NLENS)
        RADIUS   = $(RADIUS)
        OBJECT   = $(OBJECT)
    }
} 
